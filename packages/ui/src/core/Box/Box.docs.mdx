import { Meta, Description, Source } from "@storybook/blocks";

import * as BoxStories from "./Box.stories";

<h1>Box</h1> <span>Polymorphic layout primitive</span>

<p>Box is a layout primitive that acts as a wrapper component for most of the CSS utility needs.</p>

<Meta of={BoxStories} />

<Description of={BoxStories.Default} />

<Source of={BoxStories.Default} />